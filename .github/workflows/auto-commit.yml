name: Automated Contribution Engine

on:
  schedule:
    # Runs daily at 23:00 UTC (Adjust as needed)
    - cron: '0 23 * * *'
  workflow_dispatch: # Allows manual trigger from the Actions tab

jobs:
  commit-job:
    runs-on: ubuntu-latest
    permissions:
      contents: write # Crucial: gives the runner permission to push to your repo

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Run Commit Bot
        env:
          # This GitHub-provided address links commits to the 1tobiasmm account.
          GIT_AUTHOR_NAME: 1tobiasmm
          GIT_AUTHOR_EMAIL: 178795395+1tobiasmm@users.noreply.github.com
        run: python bot.py

      - name: Push Changes
        run: git push origin main
